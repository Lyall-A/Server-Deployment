version: "3.8"
services:
  TEMPLATE:
    image: TEMPLATE/TEMPLATE:latest
    container_name: TEMPLATE
    restart: unless-stopped
    network_mode: bridge
    ports:
      - 80:80
    volumes:
      - ../volumes/TEMPLATE/config:/config
